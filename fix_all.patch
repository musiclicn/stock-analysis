--- index.html
+++ index.html
@@ -374,7 +374,18 @@
   @media (max-width: 720px){
     .summary { grid-template-columns: repeat(2, 1fr); }
     .chart-wrap { height: 360px; }
-    .section-head { flex-direction: column; align-items: flex-start; }
+    .section-head { flex-direction: column; align-items: flex-start; }
+    #sectors-wrap.sectors { margin-top: 10px; }
+    #sectors-toggle { display: block !important; margin-left: 0 !important; margin-top: 8px !important; }
+    .mobile-hidden { display: none !important; }
+    .mobile-collapsed { display: none; }
+    .history-col { display: none !important; }
+    .stocks td, .stocks th { padding: 10px 4px !important; }
+    .stocks { font-size: 12px; }
   }

   footer.foot { margin-top: 40px; color:var(--text-dim); font-size:11px; text-align:center; }
@@ -611,6 +622,7 @@
     <div class="section-head">
       <h2 data-i18n="section.sectors">Sectors (SPDR ETFs)</h2>
       <div class="hint" data-i18n="hint.sectors">Sorted by active timeframe · click a sector for drill-down (prototype)</div>
+      <button id="sectors-toggle" style="margin-left:auto; padding: 4px 8px; font-size: 12px; display: none;" class="sectors-toggle">Show</button>
     </div>
     <div class="sectors" id="sectors-wrap"></div>
     <div class="legend">
@@ -702,16 +714,16 @@
         <table class="stocks" id="stocks-table">
           <thead>
             <tr>
-              <th data-k="rank" style="width:60px"><span data-i18n="th.rank">#</span></th>
+              <th data-k="rank" style="width:60px" class="mobile-hidden"><span data-i18n="th.rank">#</span></th>
               <th data-k="sym"><span data-i18n="th.ticker">Ticker</span></th>
-              <th data-k="name"><span data-i18n="th.name">Name</span></th>
-              <th data-k="sector"><span data-i18n="th.sector">Sector</span></th>
-              <th data-k="capCategory"><span data-i18n="th.cap">Cap</span></th>
-              <th data-k="volume" class="num" style="text-align:right"><span data-i18n="th.volume">Volume</span></th>
-              <th data-k="volRatio" class="num" style="text-align:right"><span data-i18n="th.volRatio">Vol Ratio</span></th>
+              <th data-k="name" class="mobile-hidden"><span data-i18n="th.name">Name</span></th>
+              <th data-k="sector" class="mobile-hidden"><span data-i18n="th.sector">Sector</span></th>
+              <th data-k="capCategory" class="mobile-hidden"><span data-i18n="th.cap">Cap</span></th>
+              <th data-k="volume" class="num mobile-hidden" style="text-align:right"><span data-i18n="th.volume">Volume</span></th>
+              <th data-k="volRatio" class="num mobile-hidden" style="text-align:right"><span data-i18n="th.volRatio">Vol Ratio</span></th>
               <th data-k="weekly" class="num" style="text-align:right"><span data-i18n="tf.weekly">Weekly</span> <span class="arrow">▼</span></th>
-              <th data-k="chg2w" class="num" style="text-align:right" title="Sum of weekly score changes over the past 2 weeks"><span style="font-size:11px; letter-spacing:.04em;">2W&thinsp;Δ</span></th>
+              <th data-k="chg2w" class="num mobile-hidden" style="text-align:right" title="Sum of weekly score changes over the past 2 weeks"><span style="font-size:11px; letter-spacing:.04em;">2W&thinsp;Δ</span></th>
               <th class="num history-col" style="text-align:right" title="Weekly score — 20 most recent data points (oldest → newest)"><span style="font-size:10px; letter-spacing:.06em; color:var(--text-dim);">←&thinsp;20W</span></th>
               <th data-k="daily"  class="num" style="text-align:right"><span data-i18n="tf.daily">Daily</span></th>
-              <th data-k="chg2d" class="num" style="text-align:right" title="(latest daily − previous daily) + (2 days ago daily − previous daily)"><span style="font-size:11px; letter-spacing:.04em;">2D&thinsp;Δ</span></th>
+              <th data-k="chg2d" class="num mobile-hidden" style="text-align:right" title="(latest daily − previous daily) + (2 days ago daily − previous daily)"><span style="font-size:11px; letter-spacing:.04em;">2D&thinsp;Δ</span></th>
               <th class="num history-col" style="text-align:right" title="Daily score — 20 most recent data points (oldest → newest)"><span style="font-size:10px; letter-spacing:.06em; color:var(--text-dim);">←&thinsp;20D</span></th>
               <th data-k="price"  class="num" style="text-align:right"><span data-i18n="th.price">Price</span></th>
               <th data-k="chg"    class="num" style="text-align:right"><span data-i18n="th.chg">Chg %</span></th>
@@ -1082,7 +1094,7 @@
         </div>
       </div>
       <div class="index-body">
-        <div class="gauge">
+        <div class="gauge mobile-hidden">
           <svg viewBox="0 0 140 140">
             <circle class="track" cx="70" cy="70" r="58" stroke-width="12" fill="none" />
             <circle class="bar"   cx="70" cy="70" r="58" stroke="${color}" stroke-width="12" fill="none"
@@ -1097,11 +1109,11 @@
         </div>
         <div class="stat-grid">
           <div class="stat"><div class="k">${t('card.price')}</div><div class="v">${fmtNum(ix.price||0,2)}</div></div>
-          <div class="stat"><div class="k">${t('card.trend')}</div><div class="v">${trendLabel(score)}</div></div>
-          <div class="stat"><div class="k">${t('card.volume')}</div><div class="v">${fmtVol(ix.volume||0)}</div></div>
-          <div class="stat"><div class="k">${t('card.vol_ratio')}</div><div class="v" style="color:${(ix.volRatio||0) > 1.5 ? 'var(--score-100)' : 'var(--text)'}">${ix.volRatio ? ix.volRatio.toFixed(2) : '—'}</div></div>
           <div class="stat"><div class="k">${t('card.daily_score')}</div><div class="v" style="color:${scoreColor(ix.daily)}">${ix.daily}</div></div>
           <div class="stat"><div class="k">${t('card.weekly_score')}</div><div class="v" style="color:${scoreColor(ix.weekly)}">${ix.weekly}</div></div>
+          <div class="stat mobile-hidden"><div class="k">${t('card.trend')}</div><div class="v">${trendLabel(score)}</div></div>
+          <div class="stat mobile-hidden"><div class="k">${t('card.volume')}</div><div class="v">${fmtVol(ix.volume||0)}</div></div>
+          <div class="stat mobile-hidden"><div class="k">${t('card.vol_ratio')}</div><div class="v" style="color:${(ix.volRatio||0) > 1.5 ? 'var(--score-100)' : 'var(--text)'}">${ix.volRatio ? ix.volRatio.toFixed(2) : '—'}</div></div>
         </div>
       </div>
       <div class="card-history history-col">
@@ -1202,26 +1214,26 @@
       return `
         <tr>
-          <td class="rank">${r._rank}</td>
+          <td class="rank mobile-hidden">${r._rank}</td>
           <td class="sym">${esc(r.sym)}</td>
-          <td class="name">${esc(r.name)}</td>
-          <td class="name" style="color:var(--text-dim)">${esc(r.sector||'')}</td>
-          <td class="name" style="color:var(--text-dim); font-size:12px;" title="${esc(r.capCategory||'')}">${r.capCategory && r.capCategory !== 'Unknown' ? esc(r.capCategory) : '—'}</td>
-          <td class="num" style="color:var(--text-soft)">${fmtVol(r.volume||0)}</td>
-          <td class="num" style="font-weight:600; color:${(r.volRatio||0) > 2 ? 'var(--score-100)' : (r.volRatio||0) > 1.5 ? 'var(--score-75)' : 'var(--text-soft)'}">${r.volRatio ? r.volRatio.toFixed(2) : '—'}</td>
+          <td class="name mobile-hidden">${esc(r.name)}</td>
+          <td class="name mobile-hidden" style="color:var(--text-dim)">${esc(r.sector||'')}</td>
+          <td class="name mobile-hidden" style="color:var(--text-dim); font-size:12px;" title="${esc(r.capCategory||'')}">${r.capCategory && r.capCategory !== 'Unknown' ? esc(r.capCategory) : '—'}</td>
+          <td class="num mobile-hidden" style="color:var(--text-soft)">${fmtVol(r.volume||0)}</td>
+          <td class="num mobile-hidden" style="font-weight:600; color:${(r.volRatio||0) > 2 ? 'var(--score-100)' : (r.volRatio||0) > 1.5 ? 'var(--score-75)' : 'var(--text-soft)'}">${r.volRatio ? r.volRatio.toFixed(2) : '—'}</td>
           <td class="num">
             <div class="score-cell">
-              <div class="score-bar"><div style="width:${wsc}%; background:${wcol}"></div></div>
+              <div class="score-bar mobile-hidden"><div style="width:${wsc}%; background:${wcol}"></div></div>
               <div class="score-val" style="color:${wcol}">${wsc}</div>
             </div>
           </td>
-          <td class="num ${d2wCls}" style="font-family:var(--mono); font-weight:600;">${d2wStr}</td>
+          <td class="num ${d2wCls} mobile-hidden" style="font-family:var(--mono); font-weight:600;">${d2wStr}</td>
           <td class="num history-col">${heatStrip(r.history_weekly||[])}</td>
           <td class="num">
             <div class="score-cell">
-              <div class="score-bar"><div style="width:${dsc}%; background:${dcol}"></div></div>
+              <div class="score-bar mobile-hidden"><div style="width:${dsc}%; background:${dcol}"></div></div>
               <div class="score-val" style="color:${dcol}">${dsc}</div>
             </div>
           </td>
-          <td class="num ${d2dCls}" style="font-family:var(--mono); font-weight:600;">${d2dStr}</td>
+          <td class="num ${d2dCls} mobile-hidden" style="font-family:var(--mono); font-weight:600;">${d2dStr}</td>
           <td class="num history-col">${heatStrip(r.history_daily||[])}</td>
           <td class="num">${fmtNum(r.price||0, 2)}</td>
           <td class="num ${chgCls}">${chg >= 0 ? '+' : ''}${chg.toFixed(2)}%</td>
@@ -1280,6 +1292,21 @@
   function bind(){
+    document.body.addEventListener('click', e => {
+      if (e.target.id === 'sectors-toggle') {
+        const sectorsWrap = document.getElementById('sectors-wrap');
+        const legend = document.querySelector('.sectors').nextElementSibling;
+        if (sectorsWrap.classList.contains('mobile-collapsed')) {
+          sectorsWrap.classList.remove('mobile-collapsed');
+          if (legend) legend.classList.remove('mobile-collapsed');
+          e.target.textContent = 'Hide';
+        } else {
+          sectorsWrap.classList.add('mobile-collapsed');
+          if (legend) legend.classList.add('mobile-collapsed');
+          e.target.textContent = 'Show';
+        }
+      }
+    });
     document.querySelectorAll('.inner-tabs button[data-idx]').forEach(b => {
       b.addEventListener('click', () => {
@@ -1308,6 +1335,11 @@
     init(){
       bind();
       load();
+      if (window.innerWidth <= 720) {
+        const sectorsWrap = document.getElementById('sectors-wrap');
+        const legend = document.querySelector('.sectors').nextElementSibling;
+        if (sectorsWrap) sectorsWrap.classList.add('mobile-collapsed');
+        if (legend && legend.classList.contains('legend')) legend.classList.add('mobile-collapsed');
+      }
       let timer = setInterval(() => load(true), POLL_MS);
       document.addEventListener('visibilitychange', () => {
         if (document.hidden) clearInterval(timer);
